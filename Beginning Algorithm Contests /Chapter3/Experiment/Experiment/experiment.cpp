#include <stdio.h>
#include <string.h>

int main()
{
	//int count = 0;
	//printf("%d %d %d\n", ++count, ++count, ++count);
	//char buf[20];
	//int abc = 773, de = 25, x = 2, y = 3;
	//sprintf_s(buf, "%d%d%d%d", abc, de, x, y);
	int i = 129;
	printf("%d\n", i);
	return 0;
}